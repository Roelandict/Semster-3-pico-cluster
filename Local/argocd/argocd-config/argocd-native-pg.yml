apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: native-pg-application
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/Roelandict/Semster-3-pico-cluster.git'  
    targetRevision: HEAD
    path: Local/postgress-nativePG
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: cnpg-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
